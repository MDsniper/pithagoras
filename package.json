{
  "name": "project",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "web",
    "docs"
  ],
  "scripts": {
    "build": "node canary.js && tsc",
    "prepare": "node canary.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}